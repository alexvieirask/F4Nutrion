import { StyleSheet } from 'react-native'

const styles_global = StyleSheet.create({
    safeAreaView:{
        padding: '5%',
        flex:1,
    
    },
    title: {
        color: "#FF6638",
        fontSize: 30,
        fontWeight: "bold"
      },
})

export { styles_global }